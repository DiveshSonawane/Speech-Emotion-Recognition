# Speech-Emotion-Recognition
Speech Emotion Recognition using Deep Learning is an AI-based system that identifies human emotions from speech signals.  The model is trained on a merged dataset consisting of RAVDESS (Speech &amp; Song) and TESS datasets, achieving high accuracy. The final system is deployed as an interactive Streamlit web application.
